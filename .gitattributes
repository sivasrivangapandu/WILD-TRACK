# LFS REMOVED: Large model files (.h5) are downloaded at runtime from GitHub Releases
# This resolves "LFS budget exceeded" errors on Render deployment
# The backend/main.py download_models_if_missing() function handles this automatically
